---
- name: Reload Systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart Grafana
  ansible.builtin.systemd:
    name: grafana-server
    state: restarted
    daemon_reload: true

