---
- name: Reload Systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart Node Exporter
  ansible.builtin.systemd:
    name: node_exporter
    state: restarted
    daemon_reload: true
