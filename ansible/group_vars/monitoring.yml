---
ansible_ssh_common_args: >-
  {% if bastion_ip | default('') != '' %}
  -o ProxyCommand="ssh -W %h:%p -q {{ ansible_user }}@{{ bastion_ip }} -i {{ ansible_ssh_private_key_file }} -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null"
  {% endif %}
